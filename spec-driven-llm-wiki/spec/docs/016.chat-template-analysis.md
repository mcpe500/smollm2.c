# Chat Template Analysis: SmolLM2-135M-Instruct

## Executive Summary

**CRITICAL FINDING: The C implementation was missing the proper chat template.**

The current CLI sends prompts like:
```
<|im_start|>hello
```

But SmolLM2-Instruct expects:
```
<|im_start|>user
hello<|im_end|>
<|im_start|>assistant
```

This explains why Ollama produces coherent text ("Hello! How can I help you today?") while the C implementation produces garbage or immediately stops.

## Token ID Reference

From tokenizer.json and model header:
- **Token 0**: `<|endoftext|>` - End of conversation context
- **Token 1**: `<|im_start|>` - Message turn start
- **Token 2**: `<|im_end|>` - Message turn end

## Test Results

### Test 1: Plain "hello" (BROKEN)

```
Input: <|im_start|>hello
Tokens: [1, 28120]

Top 10 logits after prefill:
  [198] 'Ċ' (newline)    = 27.59
  [30] '.'              = 27.34
  [288] 'Ġto'           = 26.84
  [28] ','              = 26.59
  [216] 'Ġ' (space)     = 26.56
  ...

Generated: ".<|im_end|>" (then EOS after 1 token!)
```

**Problem**: Without proper chat framing, the model sees `<|im_start|>hello` as a standalone message and immediately tries to end it. Token 2 (`<|im_end|>`) has very high probability.

### Test 2: Proper Chat Template (WORKING)

```
Input: <|im_start|>user\nhello<|im_end|>\n<|im_start|>assistant\n
Tokens: [1, 4093, 10, 28120, 2, 1, 520, 9531, 10]
        (im_start, user, \n, hello, im_end, im_start, ass, istant, \n)

Top 10 logits after prefill:
  [198] 'Ċ' (newline)    = 30.28
  [32] '0'                = 28.97
  [33] '1'                = 28.86
  [34] '2'                = 28.84
  [39] '7'                = 28.56
  ...

Generated: "\nLizzzy: Ah, yes, I know that\'s the name of..."
```

**SUCCESS**: The model produces coherent, multi-token output with realistic response structure (character name, dialogue).

## Root Cause Analysis

### Issue 1: Missing Chat Template (CRITICAL)

**Code Location: `src/smollm2.c` lines 139-145**

```c
// Current (WRONG):
tokens[n_tokens++] = 1;  // BOS token (comment is misleading!)
int n = sm2_tokenizer_encode(tokenizer, args->prompt, tokens + n_tokens, ...);
```

The code treats token 1 (`<|im_start|>`) as a "BOS token" but:
1. It's actually the message turn start marker
2. Without role and content framing, the model doesn't understand the context
3. The model was trained with full chat template supervision

### Issue 2: Tokenizer Treats Newlines as Spaces (SECONDARY)

**Code Location: `src/sm2_tokenizer.c` lines 177-179**

```c
static int is_whitespace(char c) {
    return c == ' ' || c == '\n' || c == '\r' || c == '\t' || c == '\v';
}
```

When encoding "user\n", the `\n` is treated as whitespace and converted to `Ġ` (space token) instead of being preserved as a newline. This corrupts the chat template format.

**Current (WRONG) tokenization of chat template:**
```
Input: "<|im_start|>user\nhello<|im_end|>\n<|im_start|>assistant\n"
Tokens: [1, user, Ġ, hello, Ġ, 2, 1, ass, istant, Ġ, Ġ]
       (newlines converted to Ġ spaces)
```

### Issue 3: byte_to_token Identity Mapping (BY DESIGN)

The byte_to_token array maps byte values directly to token indices (byte 0 -> token 0, etc.). This is identity mapping. Tokens 0-15 in the vocab are special tokens, NOT byte tokens, so byte 0 maps to `<|endoftext|>` which is incorrect for actual byte-level encoding.

**However**, this only affects the byte-level fallback path, not the BPE tokenizer path.

### Proper Chat Template Format

For user prompts, the format should be:
```
<|im_start|>user
{prompt}<|im_end|>
<|im_start|>assistant
```

Breaking this down:
1. `1` - `<|im_start|>` (message start)
2. Encode "user\n" - role marker with newline
3. Encode prompt text
4. `2` - `<|im_end|>` (end of user message)
5. `1` - `<|im_start|>` (assistant turn start)
6. Encode "assistant\n" - assistant role marker with newline

## Ollama Comparison

Ollama likely automatically applies the chat template based on the model name ("smollm2" or "smollm2-instruct"). Our C implementation needs to do the same.

Expected Ollama behavior:
- Detects "instruct" variant
- Wraps prompts in chat template
- Produces coherent assistant responses

## Recommended Fix

### Fix 1: Apply Chat Template (CRITICAL)

In `smollm2.c`, detect if model is an instruct variant and apply template:

```c
// Detect instruct variant (heuristic based on model path)
int is_instruct = (strstr(model_path, "instruct") != NULL ||
                   strstr(model_path, "v2") != NULL ||
                   strstr(model_path, "135m") != NULL);  // SmolLM2 variants are all instruct

if (is_instruct) {
    // Apply proper chat template
    tokens[n_tokens++] = 1; // <|im_start|>
    n = sm2_tokenizer_encode(tokenizer, "user\n", tokens + n_tokens, 4096 - n_tokens);
    n_tokens += n;
    n = sm2_tokenizer_encode(tokenizer, args->prompt, tokens + n_tokens, 4096 - n_tokens);
    n_tokens += n;
    tokens[n_tokens++] = 2; // <|im_end|>
    tokens[n_tokens++] = 1; // <|im_start|> (for assistant turn)
} else {
    // Base model - no template
    tokens[n_tokens++] = 1; // Regular BOS
    n = sm2_tokenizer_encode(tokenizer, args->prompt, tokens + n_tokens, 4096 - n_tokens);
    n_tokens += n;
}
```

### Fix 2: Handle Newlines in Tokenizer (SECONDARY)

In `src/sm2_tokenizer.c`, add special handling for `\n`:

```c
// In pre_tokenize, check for specific whitespace that should be preserved
if (*p == '\n') {
    // For chat templates, we need actual newlines
    // Add a special piece type or encode directly
    pieces[n].type = TOKEN_PIECE_NEWLINE;
    pieces[n].text = p;
    pieces[n].len = 1;
    p++;
    n++;
}

// In sm2_tokenizer_encode:
if (pieces[w].type == TOKEN_PIECE_NEWLINE) {
    // Find newline token - this should be the actual newline in the vocab
    // For now, encode as raw byte which the model can interpret
    // Actually, we need to find what token represents newline
    // Looking at the model: there might not be a direct newline token
    // The safest approach is to encode it as: <|im_end|> + <|im_start|>
    // or use a different encoding
}

// Alternative: Use byte-level token for newline
// Find the token ID for byte value 10 (newline)
```

### Fix 3: Add CLI Flag for Chat Mode

Add `-c/--chat` flag to explicitly enable chat template:

```c
typedef struct {
    ...
    int chat_mode;  // Apply chat template
} cli_args;

// Parse: --chat or -c
if (args->chat_mode) {
    // Apply template as above
}
```

## Verification

After fix, running:
```bash
./smollm2-cli -m smollm2-135m-v2.sm2 -p "hello"
```

Should output something like:
```
Hello! How can I help you today?
```

Instead of:
```
.
```

## Files Affected

- `/data/data/com.termux/files/home/smollm2.c/src/smollm2.c` - Main inference loop (line ~139)
- Need to add chat template detection/flag

## Test Script

See `test_chat_template.c` for the comparison test used to identify this issue.

## References

- SmolLM2 paper: Training efficient language models for diverse downstream tasks
- HuggingFace SmolLM2-Instruct: Uses chat template by default
- Ollama chat templates: Applied automatically per model type

## Summary: Why Ollama Works and C Implementation Doesn't

| Aspect | Ollama | C Implementation |
|--------|--------|------------------|
| Prompt Processing | Auto-detects instruct model, applies chat template | No template, just adds `<\|im_start\|>` prefix |
| Chat Template | `<\|im_start\|>user\n{hello}<\|im_end\|>\n<\|im_start\|>assistant\n` | `<\|im_start\|>{hello}` |
| Model Input | Properly formatted as assistant turn | Malformed as standalone message |
| Output | "Hello! How can I help you today?" | "." (single token then EOS) |
| Logit Distribution | Top tokens are reasonable words | Top tokens include EOS markers |

**Root Cause Chain:**
1. C implementation doesn't apply chat template
2. Model receives malformed input
3. Model predicts end-of-turn (token 2) immediately
4. Output is garbled or just stops

**Fix Priority:**
1. **HIGH**: Apply chat template (Fix 1)
2. **MEDIUM**: Handle newlines properly (Fix 2)
3. **LOW**: Add CLI flag for explicit chat mode (Fix 3)
