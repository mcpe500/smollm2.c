# Hugging Face Reference Results for SmolLM2-135M

Generated: 2026-05-29

## Test Configuration
- Model: HuggingFaceTB/SmolLM2-135M-Instruct
- Framework: transformers (via Python/PyTorch)
- Device: CPU (no GPU)

## Results

### Tokenization Test
```
'hello' -> [28120]
'hello' decoded: 'hello'
```

### Plain "hello" Input (single token)

**Input**: `[28120]` (length 1)

**Top 10 tokens at last position**:
```
    28: 0.0424 ","
   260: 0.0367 " the"
   198: 0.0297 "\n"
    30: 0.0255 "."
   288: 0.0206 " to"
   284: 0.0187 " and"
   282: 0.0180 " of"
    33: 0.0175 "1"
   253: 0.0138 " a"
   314: 0.0092 " is"
```

Note: For a single-token input, first and last positions are the same, so we only get one set of predictions. The model predicts punctuation/whitespace when given only "hello".

### Chat Template Test (KEY TEST)

**Chat prompt**:
```
<|im_start|>system\nYou are a helpful AI assistant named SmolLM, trained by Hugging Face<|im_end|>\n<|im_start|>user\nhello<|im_end|>\n<|im_start|>assistant\n
```

**Input IDs** (30 tokens):
```
[1, 9690, 198, 2683, 359, 253, 5356, 5646, 11173, 3365, 3511, 308, 34519, 28, 7018, 411, 407, 19712, 8182, 2, 198, 1, 4093, 198, 28120, 2, 198, 1, 520, 9531, 198]
```

**Top 10 tokens at last position (after assistant prompt)**:
```
 19556: 0.4998 "Hello"
 28120: 0.0719 "hello"
    57: 0.0715 "I"
  9085: 0.0266 "Welcome"
    26: 0.0252 "*"
  6004: 0.0204 "hi"
    89: 0.0172 "i"
    18: 0.0101 """
  1780: 0.0078 "What"
 12018: 0.0069 "good"
```

**KEY FINDING**: The top predicted token is **19556 "Hello"** with probability **49.98%**, NOT "." or "hello".

### Generation from Chat Template

**Output** (with temperature=0.7, max_new_tokens=20, do_sample=True):
```
<|im_start|>system
You are a helpful AI assistant named SmolLM, trained by Hugging Face<|im_end|>
<|im_start|>user
hello<|im_end|>
<|im_start|>assistant
hello! how can i help you today?<|im_end|>
```

The model generates "hello!" starting with "hello" (token 28120) as the second-most likely token.

## Implications for C Implementation

1. **Chat template matters**: The difference between plain "hello" and chat template is HUGE. The chat template includes system prompt and proper turn structure.

2. **Top prediction is "Hello" (token 19556)**: If the C implementation produces "." or something other than "Hello", there's a forward pass bug.

3. **Softmax over logits**: The probabilities are computed via `torch.softmax(logits, dim=-1)`, so logits should be raw unnormalized scores.

4. **Greedy vs Sampling**: When generating, the model can pick "hello" (token 28120, 7.19%) instead of "Hello" (49.98%) depending on sampling parameters.

## Files Tested
- Script: `/data/data/com.termux/files/home/smollm2.c/test_python_ref.py`
- Tokenizer: `tokenizer.json` from HuggingFaceTB/SmolLM2-135M-Instruct
- Config: `config.json` from HuggingFaceTB/SmolLM2-135M-Instruct